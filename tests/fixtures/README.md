# Fixtures

Invented data in the exact shape each vendor writes.

These files exist so the readers can be tested against real formatting —
quoted fields, rest-timer rows, per-meal rows, timestamps that repeat down a
session — without any health record ever entering this repository. Nothing here
describes a real person. When a vendor changes its export, copy the *shape* of
the new file and write fresh numbers; never paste an actual export in.
